package org.midgard.ragnarok.loginserver.app;

import org.midgard.ragnarok.app.AbstractCliOptions;

/**
 * CLI options configuration for login server.
 */
public class LoginCliOptions extends AbstractCliOptions {
    public LoginCliOptions() {
    }
}
